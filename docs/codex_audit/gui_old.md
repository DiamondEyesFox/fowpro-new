# Legacy GUI (fowpro/gui_old.py)

## Summary
- Older, monolithic GUI implementation (pre `fowpro/gui/*`).
- Contains earlier layout logic, event handling, and UI rendering for the engine.

## Status
- Not used by current `main.py` entrypoint (current UI uses `fowpro/gui/app.py`).
- Useful as a historical reference but likely out-of-sync with current engine/hooks.
